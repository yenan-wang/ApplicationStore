<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.applicationstore">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TextActivity"></activity>
        <activity
            android:name=".SignActivity"
            android:label="@string/sign_activity_name" />
        <activity
            android:name=".SettingActivity"
            android:label="@string/setting_activity_name"
            android:theme="@style/SettingTheme" />
        <activity
            android:name=".InstallPage"
            android:label="@string/install_activity_name" />
        <activity
            android:name=".MoreActivity"
            android:label="@string/more_activity_name" />
        <activity
            android:name=".SearchActivity"
            android:label="@string/search_activity_name" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>